<script>
	import Icon from '@svizzle/ui/src/icons/Icon.svelte';
	import List from '@svizzle/ui/src/icons/feather/List.svelte';
	import MapPin from '@svizzle/ui/src/icons/feather/MapPin.svelte';
	import Sliders from '@svizzle/ui/src/icons/feather/Sliders.svelte';
	import Tag from '@svizzle/ui/src/icons/feather/Tag.svelte';
	import {noop} from '@svizzle/utils';

	import City from 'app/components/icons/City.svelte';
	import Region from 'app/components/icons/Region.svelte';

	export let setView = noop;
	export let activeViewId;

	const getColor = (viewId, activeView) =>
		viewId === activeView ? 'white' : 'black';
</script>

<nav class='ViewSelector'>

	<!-- settings -->
	<div
		class:active={activeViewId === 'settings'}
		class='button clickable'
		on:click={setView('settings')}
	>
		<Icon
			glyph={Sliders}
			stroke={getColor('settings', activeViewId)}
		/>
	</div>

	<!-- map -->
	<div
		class:active={activeViewId === 'map'}
		class='button clickable'
		on:click={setView('map')}
	>
		<Icon
			glyph={MapPin}
			stroke={getColor('map', activeViewId)}
		/>
	</div>

	<!-- details -->
	<div
		class:active={activeViewId === 'details'}
		class='button clickable'
		on:click={setView('details')}
	>
		<Icon
			glyph={List}
			stroke={getColor('details', activeViewId)}
		/>
	</div>

	<!-- topics -->
	<div
		class:active={activeViewId === 'topics'}
		class='button clickable'
		on:click={setView('topics')}
	>
		<Icon
			glyph={Tag}
			stroke={getColor('topics', activeViewId)}
		/>
	</div>

	<!-- places -->
	<div
		class:active={activeViewId === 'places'}
		class='button clickable'
		on:click={setView('places')}
	>
		<Icon
			fill={getColor('places', activeViewId)}
			glyph={City}
			stroke='none'
		/>
	</div>

	<!-- regions -->
	<div
		class:active={activeViewId === 'regions'}
		class='button clickable'
		on:click={setView('regions')}
	>
		<Icon
			glyph={Region}
			stroke={getColor('regions', activeViewId)}
			strokeWidth=1.25
		/>
	</div>
</nav>

<style>
	.ViewSelector {
		background-color: white;
		border-top: 1px solid var(--colorMain);
		display: grid;
		grid-template-columns: repeat(6, 1fr);
		grid-template-rows: 100%;
		height: 100%;
		width: 100%;
	}
	.active {
		background: var(--color-selected);
		color: white;
	}

	.button {
		align-items: center;
		align-self: stretch;
		display: grid;
		justify-items: center;
		justify-self: stretch;
		padding: .5em 0;
	}
	/* .rotated {
		transform: scaleX(-1) rotate(-90deg);
	} */

	.clickable {
		cursor: pointer;
	}
</style>
