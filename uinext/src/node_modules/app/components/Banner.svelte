<script>
	import { createEventDispatcher } from 'svelte';

	import BodyFooter from 'app/components/BodyFooter.svelte';

	export let _screen;
	export let isWide = false;
	export let footerText;

	const dispatch = createEventDispatcher();
	const close = () => {
		dispatch('close');
	}
	$: footerText = footerText || 'Click on background to dismiss';
</script>

<div
	aria-label="Banner"
	class='Banner clickable {$_screen?.classes}'
	role='alert'
	on:click={close}
>
	<div class='inner' class:narrow={!isWide} on:click|stopPropagation>
		<BodyFooter {footerText}>
			<slot name='body' slot='body' />
		</BodyFooter>
	</div>
</div>

<style>
	.Banner {
		background-color: rgba(0, 0, 0, 0.25);
		box-shadow: var(--box-shadow-xy);
		height: 100%;
		left: 0;
		position: absolute;
		top: 0;
		user-select: none;
		width: 100%;
		z-index: var(--z-2000);
	}
	.inner {
		background: white;
		border-radius: 1rem;
		border: thin solid var(--color-grey-70);
		box-shadow: var(--box-shadow-xy);
		display: grid;
		grid-template-rows: 1fr;
		left: 50%;
		max-height: 90%;
		padding: 0.5rem;
		position: absolute;
		overflow: auto;
		top: 50%;
		transform: translate(-50%, -50%);
		width: 90%;
		cursor: initial;
	}
	.medium .inner {
		min-width: 50%;
	}

	.medium .narrow {
		width: min-content;
	}
</style>
