<script>
	import mapboxgl from 'mapbox-gl';

	import MapboxGL from './MapboxGL.svelte';
	import MapboxJs from './MapboxJs/MapboxJs.svelte';

	const isMapboxGLSupported = mapboxgl.supported();

	export let accessToken;
	export let CustomLayers;
	export let items = [];
	export let styleURL;
	export let withScaleControl = true;
	export let withZoomControl = true;
</script>

<!-- {#if isMapboxGLSupported} -->
	<!-- <MapboxGL
		{accessToken}
		{CustomLayers}
		{items}
		{styleURL}
		{withScaleControl}
		{withZoomControl}
	/> -->
<!-- {:else} -->
	<MapboxJs
		{accessToken}
		{CustomLayers}
		{items}
		{styleURL}
		{withScaleControl}
		{withZoomControl}
	/>
<!-- {/if} -->
