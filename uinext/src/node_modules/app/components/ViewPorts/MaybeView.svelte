<script>
    import {getContext} from 'svelte';

    const {
        _currentViewId,
        registerView,
        renderAnyway
    } = getContext('views');

	export let id;

    registerView(id);
</script>

{#if renderAnyway || id === $_currentViewId}
    <slot />
{/if}
