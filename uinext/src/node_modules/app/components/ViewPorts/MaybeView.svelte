<script>
    import { getContext } from 'svelte';

    const {
        _currentViewId,
        addView,
        renderAnyway
    } = getContext('views');

	export let id;

    addView(id);
</script>

{#if renderAnyway || id === $_currentViewId}
    <slot />
{/if}
