<script>
	import {getContext} from 'svelte';

	const {
		_currentViewId,
		registerView,
		viewType
	} = getContext('viewport');

	export let id;

	$: doRender = viewType !== 'xor';

	registerView(id);
</script>

{#if doRender || id === $_currentViewId}
	<slot />
{/if}
