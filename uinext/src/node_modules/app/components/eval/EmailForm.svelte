<script>
	import {_evaluatorSettings} from 'app/stores/eval';
	import Input from '../Input.svelte';

	let userEmail = '';

	const onSubmitEmail = () => {
		$_evaluatorSettings = {userEmail};
		console.log($_evaluatorSettings)
	};
</script>

<div class='emailForm'>
	<Input
		autofocus=true
		bind:value={userEmail}
		on:submitted={onSubmitEmail}
		placeholder='Please insert your email'
		type='email'
	/>
	<button on:click={onSubmitEmail}>Start</button>
</div>

<style>
	.emailForm {
		align-items: center;
		column-gap: 0.5em;
		display: grid;
		height: 100%;
		justify-items: center;
		grid-template-columns: 1fr auto;
		grid-auto-flow: column;
		padding: 1em;
		width: 100%;
	}
	button {
		border: thin solid var(--color-grey-70);
		border-radius: 0.125em;
		padding: 0.25em;
		font-size: 1em;
	}
</style>
