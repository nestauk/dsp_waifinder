import { createMachina } from '../utils';

import { selectingOptions } from 'app/machines/map/route.options';
import { historyOptions } from './history.options';
import { routeConfig } from './route.config';
import { createEditingStores } from 'app/machines/map/route.context';

export const options = {
	actions: {
		...selectingOptions.actions,
		...historyOptions.actions,
	}
};

export const createMapMachine = () => createMachina(
	routeConfig,
	options,
	createEditingStores()
);
