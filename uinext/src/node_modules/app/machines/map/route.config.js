import { editingConfig } from 'app/machines/builder/editing.config';
import { historyConfig } from './history.config';

export const routeConfig = {
	id: 'MapRoute',
	initial: 'PageLoading',
	states: {
		PageLoading: {
			on: {
				READY: {
					target: '#MapRoute.PageInteractive',
					actions: [
					]
				}
			}
		},
		PageInteractive: {
			type: 'parallel',
			states: {
				Editing: editingConfig,
				History: historyConfig,
			},
			on: {
				ROUTE_CHANGED: {
					target: '#MapRoute.PageInteractive',
					actions: [
					]
				}
			}
		}
	}
};
