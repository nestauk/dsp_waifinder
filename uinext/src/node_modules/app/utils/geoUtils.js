import {makeToGeoPoints} from '@svizzle/geo';
import * as _ from 'lamb';

const pickCoordinates = _.collect([
	_.getKey('Longitude'),
	_.getKey('Latitude')
]);

export const toGeoPoints = makeToGeoPoints(pickCoordinates);
