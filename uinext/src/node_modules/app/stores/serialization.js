import {derived} from 'svelte/store';

import {_bboxOrgs} from './data';
import {_hero} from './interaction';
import {_activeViewId} from './navigation';
import {
	_orgSearchValue,
	_orgTypesSelectionMode,
	_placesSearchValue,
	_selectedOrgTypes,
} from './selection';
import {_activeTopicDetails} from './topics';

export const _appState = derived(
	[
		_activeTopicDetails,
		_activeViewId,
		_bboxOrgs,
		_hero,
		_orgSearchValue,
		_orgTypesSelectionMode,
		_placesSearchValue,
		_selectedOrgTypes,
	],
	([
		activeTopicDetails,
		activeViewId,
		bboxOrgs,
		hero,
		orgSearchValue,
		orgTypesSelectionMode,
		placesSearchValue,
		selectedOrgTypes
	]) => ({
		activeTopicDetails,
		activeViewId,
		bboxOrgs,
		hero,
		orgSearchValue,
		orgTypesSelectionMode,
		placesSearchValue,
		selectedOrgTypes
	})
);
